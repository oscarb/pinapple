<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="alert_dialog">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="passcode_field_defaults">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:background">@color/colorPasscodeFieldBackground</item>
        <item name="android:digits">0123456789</item>
        <item name="android:inputType">numberPassword</item>
        <item name="android:contentDescription">Four digit passcode field</item>
        <item name="android:ems">6</item>
        <item name="android:fontFamily">monospace</item>
        <item name="android:lines">1</item>
        <item name="android:textSize">24sp</item>
        <item name="android:padding">16sp</item>
    </style>

    <style name="passcode_field" parent="passcode_field_defaults">

    </style>


</resources>
